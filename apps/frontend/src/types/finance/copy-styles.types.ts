import { Padding } from "./padding.types";

export interface CopyStyles {
  fontSize: string;
  fontFamily: string;
  linkColor: string;
  maxWidth: string;
  lineHeight: string;
  padding: Padding;
  bgColor: string;
}
