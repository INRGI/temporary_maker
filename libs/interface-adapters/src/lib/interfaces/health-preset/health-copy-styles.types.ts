import { HealthPadding } from "./health-padding.types";

export interface HealthCopyStyles {
  fontSize: string;
  fontFamily: string;
  linkColor: string;
  maxWidth: string;
  lineHeight: string;
  padding: HealthPadding;
  bgColor: string;
}
